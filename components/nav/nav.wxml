<view class="container">
    <image src="{{index===first?pre01:pre02}}" />
    <text class="title">{{title}}</text>
    <image src="{{index===lastest?next01:next02}}" />
    <!-- 增大点击面积 -->
    <view class="btn pre" bind:tap="pre"></view>
    <view class="btn next" bind:tap="next"></view>
</view>